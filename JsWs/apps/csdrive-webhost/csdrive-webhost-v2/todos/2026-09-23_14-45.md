1. Regarding text editors: have a global settings option to toggle the wrapping of text lines (let's leave it enabled by default).

2. Change regarding manually closing a secondary window or an app tile: instead of removing its window entry from admin app, let's just show that entry as suspended.

3. Let's implement the new features from docs\features\features.md; after implementing those under the "New Features" heading, move them under the "Implemented Features" heading (and keep this habit).

4. Regarding app security warnings: fill the 3 headings at the end of that file with the corresponding warnings (and keep this habit).

6. I tried to open an external link once an the entire app froze completely then an Android popup said the same and asked me if I wanted to stop the app. Also, regarding the fact that initially I wanted to open all external links in our own secondary windows: let's give the option to the user to open them in the default browser (an additional button in the same OS native prompt would be ideal).

7. The app doesn't treat files with extension .mkv as video files (when I clicked on one I got a message saying "this file is too big to be edited", which made me think the default action when clicking on a file is to open it in the text editor, but that should not be the default anyway; let's keep that as an option for any file, but that should be the default only for known text file extensions - .txt, .md and programming code files; for other file extensions show a specific error message). Back to the video files: files with extension .mkv should be playable as video files.

8. The explorer of note files should be a page of its own (not a popup modal) and should function similar to the File Manager, only scoped to the files and folders of that particular note.

9. About opening a note as a web app from the list of notes: it should open in the reusable window that is reused when the its being opened from the note editor (note editor opening the note as a web app).

10. Include all keyboard shortcuts in our help page. Also, the about/help page should have an autohiding top header that contains an icon button for showing the table of contents in a popup modal (items in the TOC will be links to content in the page). While in the help tab page, pressing the same tab head again will show the top header.

11. I just noticed a slight lag on the text editor (I'm editing a large text file, maybe it matters) on every key stroke. I think it's due to the keypress handler you might have attached in order to keep track of the current text selection / caret position and to highlight the code. Of course we have to keep that, but if you attached the handler in a blocking way (I think that is default -> the browser waits for the handler's execution before it performs the default action - in order to be able to prevent the default action if the handler requests for that), let's try to attach it in a non-blocking way. Or is it the global keyboard shortcuts handler which MUST attach the handler in a blocking way? After all, this is not a blocking issue, but a nive to solve. Just to be sure: the handles for keyboard shortcuts and key stroke on text editor are all on the frontend, right? And no attach/detach handlers on every stroke, right? One last worry of mine is that there's a react rerender on every stroke. In that case we should find a way to bypass the react component state for the text editor. Lastly, I saw that this lag varies in severity and sometimes doesn't happen at all. It seems to happen as I spend more time in the editor and if I leave it alone for a little while then come back to it, it's often as fast as any text editor. So it's worth investigating a potential memory leak in ou react app or something similar.

12. Nice to have: on windows 11 you helped me develop the following app: c:\A\T\ayran\Apps\taskbar-shortcuts\taskbar-shortcuts-v1; also, I opened my CsDrive Webhost app with multiple windows and had one of them docked to half the screen (using the Win+right-arrow shortcut) and when I refocus it with the mouse from the taskbar, it stays in place. But when I refocus it using the Ayran Taskbar Shortcuts app, it snaps out of the dock and instead of taking up the right half of the screen it is now centered to the screen taking up a fourth of the screen. Can you find a way (maybe change something in Taskbar Shortcuts app) to prevent the window from losing its dock-to-half-screen when picked up by the Taskbar Shortcuts app? Note, I tried the same thing on totally different app (a pdf viewer), that is to pick the docked-to-half-screen pdf viewer using the Taskbar Shortcuts app and the pdf viewer stayed in place as desired. So I'm not certain whether the bug is in the Taskbar Shortcuts app or in the CsDrive Webhost app.

13. For all text editors add a global option to show white spaces (replace all non-space white spaces with a suggestive unicode symbol and all white spaces, including the space, should have a distinct background color when this option is on). Remember that when copying text to clipboard, the copied text should not include these visually added symbols.

14. For the Notes app File Manager and note files explorer view add an option to dump the OS clipboard to an image file (like when a screenshot has just been copied to clipboard).

15. Move all text editors and file viewer from popup modal to a dedicated page.

16. In the note editor and markdown file editor offer an option to insert a relative (or absolute) file path by opening a popup modal with an explorer view (options to explore notes and / or files - first explore notes to pick a different note, then explore files of that note; or directly all files from that storage root). Remember: the absolute path will be relative to the notebook root if its a note editor or a note file editor, or relative to the storage root if not scoped to a notebook.

17. Add line numbers to all text editors (and offer a global option to disable that).

18. About top bar autohide: its great that it hides when the user scrolls down the rest of the content. But what if the rest of the content never overflows the screen because the rest of the content itself is just a container with its own header under which lies its own nested scrollable content (like our text editors)? In this case, scrolling the text editor should auto hide both the global top bar and the editor's top bar. Likewise, when showing the global top bar from the admin app, the editor's top bar should also be shown. Last but not the least, about auto-showing back the header when the user scrolls up: it should also auto show back when the scrollable content changes so that it no longer overlaps the screen, if that's not already covered.

19. Bug regarding the reusing the window for the syncing note as a web app: I opened a note editor in a window, pressed the icon for launching the note as an app syncing and it did so in a new window, then went back to the first window, pressed the back button, created a new note, then opened it as a web app syncing and it did so in a third window. What it should do instead is reuse the second window and open there.

20. About markdown highlighting: it looks and works great, but headings inside quoted blocks are not highlighted as such and they should be.