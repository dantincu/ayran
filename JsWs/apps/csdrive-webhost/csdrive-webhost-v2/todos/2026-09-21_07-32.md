1. All the constants I ever mentioned about folder pairs and notes should be put in a config file that will be included in the app bundle / installer file.

2. In the Notes File Manager let's add the option to "Add Folders Pair" which will give the user the option to choose a custom index and even the entire short folder name (any string allowed as a folder name). This will include the .keep file inside the full folder name. In the dialog box for creating the pair there will be the title textbox. Instead of simply dissalowing invalid path chars, strip them from the full name dir upon creation, just like we do when we create notes (and show an icon hint that displays invalid path chars). Also show a checkbox for creating a markdown file inside the short folder name (just like we do when we create notes).

3. The Notes File Manager and Note Files Explorer should allow easy file and folder (and note) renaming the way Total Commander does: a keyboard shortcut (or the regular mouse/touch way to trigger it) for renaming an item, then pressing the up or down keyboard arrow will both submit the renaming and advance to the prev/next item in the list. Offer the same approach when editing note indexes (I think I did mention that we need to allow direct editing of note indexes and even display them by default. We should have a popup modal that lets us reassign indexes for all child notes of a parent note and then when the user submits, it performs that operation once for all child notes). Also, we should offer the functionality to normalize child note indexes (respecting the index intervals where they currently sit). Also, let's offer options to "convert a note's index" from one interval to another. This should be done inside the mass index reassignment popup modal. Also, when moving / removing child notes, give the option to normalize the indexes of remaining notes after completing the move / remove operation. Also, when moving / copying notes under a different parent note, by default we'll assign them the next available indexes from the new parent note. But also give the option to assign custom indexes (fill gaps in the parent note).

4. Regarding reassigning note indexes: besides manually editing the numbers, let's offer a classic reordering functionality like we do everywhere else in our app's lists: through drag and drop and with top arrow buttons. I think its easiest to combine all these index-related operations in one popup modal: manual reassignment of indexes, index interval conversion, index normalization, reordering through drag and drop and top arrow buttons. NOTE: if it's too difficult to automatically reassign indexes when dragging and dropping or moving with top arrow buttons, then simply recalculate all indexes for all child notes of the parent note upon any classic reordering operation (without submitting it instantly, of course).

## Follow-up 1:

1. Inside the "docs" folder, besides the "strategies" folder, I've just created 2 more folders:
   - one called "private", where I keep super important information about my own personal setup for this app. This file is and will always be gitignored.
   - one called "private-template", where I keep templates for the super important information from the previous folder, in order for me to easily reconstruct the "private" folder on a new machine (so this folder is not and will never be gitignored).

2. Let's implement a search functionality for the Notes File Manager and Note Files Explorer: basic search capabilities: search by item name (file name/ folder name/ note title), by contents, by last updated or created time stamps, by extension and file size for files. Require intervals for time stamps and file sizes. One edge of any interval can be ommitted (represents infinity). For all string searches add an option to interpret the search term as regexp. When searching contents with regexp, also give options to search line by line or apply the regexp to the entire contents, so the user will have to include the newlines in the regexp. Allow the user to specify the search depth and allow infinite depth (by default the depth is 1). Besides search, let's add sorting functionality (sort by name alphabetically, sort files by extension alphabetically, sort by time stamps, file size). All sorting can be ascending or descending. When applying any searching or sorting when listing notes, operations for reordering / index reassigning / index conversion operations should not be allowed. Back to searching: when searching deeply, the total number of results might really be huge enough so that simply fetching the items metadata might be too much for the system's resources. So find a way to never keep all that in memory. If too difficult group search results by children, but flatten them while still offering a way to view the "ancestors path" (this will be easy for files and folders - as it will simply be the file path, but it will be a bit more difficult for notes).

## Follow-up 2:

1. Regarding branches on the Notes App's Filen explorer: when we change it in one tab, right now it changes everywhere in our app right? What I want is to have this change persisted per window: changing the branch in one window shouldn't change it in other windows. Opening a new window will land on the main branch. A question: what happens when we change the branch in one tab and then return to a tab in the same window where we already made some edits to a file that has a different version in the new branch? We should not allow such things, unless the other tab moved from the main branch to a new branch (allowing this exception is important for me).

2. For every cached item in the Notes App, let's add options for soft refresh (refetch what we have in the cache if that is still valid), hard refresh (refresh the cache) and clear cache. These options should be available for every single item in every one of our lists, plus all our editors and media file viewers, just like we have the option to lock a file in branches. Hard refreshing a file that is being edited should not be allowed. I did mention that if we edit a file and then we change the tab without saving the edited file first, the changes should not be lost, right?

3. Let's add undo and redo buttons for file and note editors.

4. I just got an alternative idea to user action web app iframe (and tell me if it comes with better security): let's open a separate window just for that and always reuse that window (do not close it when it goes out of scope, just send an event to it to make it go to its default state - this will be handled by the user provided code). Just give an option to the user to close the window from where it was opened and also bring it to front if the user presses the user action button again. We should have a reusable control that exposes 2 buttons: 1 for launching it, and 1 for closing it. In addition to the event sent to it when it goes out of scope (the user navigates away from the resource that opened it), let's also send events whenever the user launches it. That event will contain the resource identifier that opened it. If it was closed when the user launches it, wait for the init request and only then send the event. Lastly, add these launching/closing buttons everywhere we have text inputs in our Notes App - and the launched event should contain information about the originating context. And also, for every text input in our Notes App, let the text input to interact with the internal clipboard (both paste from the internal clipboard and copy selected text to internal clipboard).

5. I've just realised that our thumbnails view only has "bt" and "t" folders for the Filen Explorer. For the File Manager I never mentioned where we should put the "t" folder. My guess is that we should generate some guids for each root and persist them in our sqlite database (and also display these guids where we list our roots, or give an option to view them - like an icon button opening a popover or when triggering the context event on the root pills; that last one is my preferred solution). Back at the "t" folder (where should it live): where exactly do the "c", "b" and "bt" (are there any other 1 or 2 letter folders I'm forgetting?) folders for the Filen accounts live? More exactly where do the Filen account folder pairs live? If they live directly inside the "files" folder, then just like these Filen folder pairs have their full name parts like "filen@@email@@userid" (or something similar), we can have full name part for our roots something like "local-fs@@rootid".

6. For the Notes App I've just realised that it would be cool to have the feature of branches for the File Manager too. Of course there won't be any caching mechanism for these File Manager branches, so we'll only have the edited files in the "b" folder for the File Manager.

7. Add some alternative themes for the entire app (including the admin app). I want one that gives the feeling of hot desert landscape. Feel free to be creative and make multiple altenatives of this kind. Then create more:
   - multiple alternatives for scorching earth 
   - multiple alternatives for tropical volcanoes.
   - multiple alternatives for sunny autumn landscapes.
   - multiple alternatives for cloudy and rainy autumn lanscapes.
   - multiple alternatives for cloudy and misty swamps.
   - multiple alternatives for heavy snow with mist.

  Changing themes should only be done from the admin app and should be persisted globally. As for updating it instantly in all secondary windows, I'm sure that's straightforward in the system apps and in the user apps it doesn't even apply. Since we've touched this aspect, let's also give an option to manually toggle the dark mode on and off (again only from the admin app). Propagating that through our system apps is likewise straightforward, but propagating that through user apps requires sending an event that the user apps can choose to listen to or not. Lastly, let's change the default theme color from blue to orange (and the color of the icon should likewise be orange).

## Follow-up 3:

1. Add more themes:
   - one for red clay
   - one for rust
   - one for copper
   - one for arabica coffee
   - one for chocolate
   - one for gold
   - one for silver
   - one for platinum
   - one for ruby
   - one for magenta
   - one for violet
   - one for velvet
   - one for cherry
   - one for pink
   - one for teal
   - one for dark teal
   - multiple for green forest
   - multiple for blue
   - multiple for steel blue
   - one for gun metal
   - multiple for wood
   - multiple for seaweed
   - multiple for mars

2. Make the theme picker section collapsible/expandable. It should be collapsed by default. Or even better: show it inside a popup modal.

3. Add an option to rotate through all. Let the user choose the rotation mode: ascending, descending, random. Let them choose the rotation interval too: secondly (how many seconds) or minutely (how many minutes) or hourly (how many hours) or daily (how many days). Don't disable the theme picker while alternating. Question: what would be the minimum interval that won't be bad for the eyes?

4. Change the name of the app from "CsDrive WebHost" to "Ayran CsDrive WebHost".

## Follow-up 4:

1. Add an option to the user to add custom themes - they can choose the 3 base colors but put some constraints so to make sure the app remains usable when they apply their custom theme. Then offer the rotation option to go only through the custom themes, only through the system themes, or through all. Also offer to generate random themes that the user can opt to save them as custom themes. Also offer to alternate through randomly generated themes (generate a random theme on the spot at each interval) - in this mode the user will have the option to save the currently applied random theme as a custom theme (and remember the previously generated random theme too - maybe the theme changed just before the user wanted to save it).
