I tested the app on both Android and Windows and here are the issues:
- Can't change app data folder on android (we did create a folder picker in kotlin, didn't we?)
- Notebook lists only 2 of my top notes in filen on both android and windows
- For everyone of our lists, let's give the user an option to move any of the icon buttons, that sit inline for each item in the list, in a popover menu triggered from a 3-dots icon button that always sits inline for each item in the list (and have this as a user setting persisted globally)

## Follow-up 1:

Regarding our popovers: right now when they are open we can still interact with the background (and we need to press on the background somewhere in order to close them). Let's change that by adding a fully transparent overlay that covers everything and sits just underneath the popover and pressing on that one will close the popover.

## Follow-up 2:

Regarding future installations on Android (let's skip it for the current one): everytime I install a Tauri app on Android, I end up with 2 icons because I have dual messenger option enabled on my Android. Let's make it that it will only install it for the default user profile.

## Follow-up 3:

1. The notes list doesn have a hard refresh and clear cache options. All our cache lists and viewers/editors should have the 3 refresh options: soft refresh, hard refresh and clear cache.

2. When creating a note, there needs to be an option for choosing the index interval.

3. About the top bar added to the note as a web app and putting it inside an iframe: let's extend this functionality to user web apps (html and markdown files).

4. All windows open in the Notes App (including the notes, html and markdown files as web apps) should have options to suspend itself or to close its tab (so put such these icon buttons on all pages of the Notes App - including the notes, html and markdown files as web apps). Also, for every tab, let's add an option to show the top bar.

5. I just thought of an alternative to the top bar for notes, html and markdown files as a web apps: let's use Android's back button to go back to the prev page (and leave the suspend and close tab to the system app management).

## Follow-up 4:

1. From prev follow-up:
   - The layout bug got fixed, that's great.
   - The popovers still don't have an overlay preventing the interaction with the background.
   - Listing the notes for the first time is ok now, but I still don't see a "files" folder in the custom app data folder.
   - Just remember about the top bar: don't add an iframe anymore

2. In the admin app let's create an about+help page to help the user use our app. There let's put some useful code snippets and instructions so that the user can easily create web apps that communicate with our rust backend. Among the snippets let's include a sample html page that shows a top toolbar with items I mentioned at the no longer implemented steps 3 and 4 from the previous follow-up. This sample html should have a clear separation between the code that will be included by the user when creating their own pages vs actual examples in that help sample html. Back at the general help documentation in this page (pages), let's extend this documentation to cover all the api our rust backend exposes. Lastly, we should never forget to update this help module whenever our rust api changes.

3. Besides handling the Android back button, let's also partially implement the points 3 and 4 from the previous follow-up, but not by adding an iframe, but similar to the way you suggested (and only for the Notes app pages - no user apps, not even the note markdown as a web app) by simply adding that closable header on top. Then let the user apps add their own header of this kind. Lastly, for each tab listed in the admin app, add an option to show the top bar (will send an event to the web pages and the Notes app pages will listen to and handle that event). Also add a global option to show the top bar for all web apps with one click.

4. We need to be able to edit code files: .html, .md, .css, .json, .js. Add color based syntax highlighting for all of these (including the note editor - there the only highlighting I saw was bolding of the md bold segments).

## Follow-up 5:

1. About preserving the scroll when syncing the markdown edits with its rendered web app: when the scroll is at the bottom, keep it at the bottom on every sync.

2. Regarding the top bar for web apps:
   - it should have a similar look to the rest of the app
   - use icon buttons instead of text buttons
   - don't forget the back button and the close top bar button
   - don't forget the bring back top bar buttons on every tab and the bring back all top bars global button from the admin app's system apps tab page
   - besides the 4 buttons, let's also include what we show for every tab in the admin app: the 2 rows of formatted text, the labels and the root (root itself with its labels) - without the manage labels button, so without javascript in this part. About how the user web apps can handle this insertion part: at the updateResource request's response, send the markup that the web app should insert in the top bar as a string (this mechanism can be used by the system web apps too). Also offer settings options in the admin app to hide tab labels and / or the tab's root in the web apps top bar. Also add a global option (enabled by default) to autohide the top bar. Then going back to the admin app's system app's page and clicking the show top bar button will instantly show it if it got hidden by autohide or closed by the user. Finally, when the user closes the top bar, let's persist that as a tab state (will be reverted when the user clicks the show top bar button from the admin app). Don't forget to update the sample html snippet for creating web apps with a top bar.

3. Regarding text editors: have a global settings option to toggle the wrapping of text lines (let's leave it enabled by default).

4. Change regarding manually closing a secondary window or an app tile: instead of removing the its window entry from admin app, let's just show that entry as suspended.

5. Let's implement the new features from docs\features\features.md; after implementing those under the "New Features" heading, move them under the "Implemented Features" heading (and keep this habit).

6. Regarding app security warnings: fill the 3 headings at the end of that file with the corresponding warnings (and keep this habit).