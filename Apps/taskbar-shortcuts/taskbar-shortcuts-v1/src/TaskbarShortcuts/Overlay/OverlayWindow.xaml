<Window x:Class="TaskbarShortcuts.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:ClassModifier="internal"
        Title="Taskbar Shortcuts"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="Manual"
        Top="40"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        Deactivated="OverlayWindow_Deactivated"
        PreviewKeyDown="OverlayWindow_PreviewKeyDown"
        Loaded="OverlayWindow_Loaded"
        ContentRendered="OverlayWindow_ContentRendered"
        MinWidth="360">
    <Border Background="#F0202020"
            BorderBrush="#3F9FCF"
            BorderThickness="1"
            CornerRadius="8"
            Padding="0,0,0,10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.6" Color="Black" />
        </Border.Effect>
        <StackPanel>
            <Border Background="#2A2A2A" CornerRadius="8,8,0,0" Padding="16,10">
                <TextBlock Text="Type a number and press Enter (add a letter for grouped windows)"
                           Foreground="#CCCCCC"
                           FontSize="13" />
            </Border>

            <ScrollViewer x:Name="ListScroller"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="WindowsList" Margin="6,8,6,4">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="Row"
                                    Padding="10,6"
                                    Margin="0,1"
                                    CornerRadius="4"
                                    Background="Transparent"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="Row_MouseLeftButtonUp">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                                <Setter Property="Background" Value="#3F5FA0D0" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsPlaceholder}" Value="True">
                                                <Setter Property="Opacity" Value="0.55" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="46" />
                                        <ColumnDefinition Width="24" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding SelectorCode}"
                                               Foreground="#3FCF7F"
                                               FontWeight="Bold"
                                               FontSize="16"
                                               VerticalAlignment="Center" />
                                    <Image Grid.Column="1"
                                           Source="{Binding Icon}"
                                           Width="18" Height="18"
                                           VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding Title}"
                                               Foreground="White"
                                               FontSize="14"
                                               Margin="10,0,0,0"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock x:Name="BufferText"
                       Margin="16,6,16,0"
                       Foreground="#3FCF7F"
                       FontSize="18"
                       FontWeight="Bold" />
        </StackPanel>
    </Border>
</Window>
