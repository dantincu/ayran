1. I just realised there could be a possible inconsistency about how we save the state for pages where we have paginated list because we change the page size globally everywhere: let's say we go to a place and navigate to a certain page having a certain page size. Then we move to a different place, change the page size, then come back to the first place, we have a different page size than was when we saved the state for this first place. Given the new page size, what is the page number that is restored here? If its the same page number that was stored when the page size was different, we'll see different records, that is unwanted behavior. If that is the case, let's find a universal way to save states for paginated lists: either save the number of records skipped instead of the current page number, or save both the page number and the page size at the moment of saving the page number.

2. Let's add the option for user and system apps to open external web sites: let's also do that inside windows of our apps (by showing an OS native confirmation box where we show the url to be opened and only one such confirmation box can be shown at a time, without pipe-ing such requests, but by notifying our web apps when the user has confirmed/denied the request to open an external website, and also notify our web apps about url changing and fetch the title from the page when available and when it changes), but not list them as tabs, like we do with our internal web apps. Instead, because any external web site will have been opened through a ceratin internal web app tab, list them as external web sites as children of the initiating tab. So we'll have one more level of nesting in our web apps navigation system. Let's also give the option to reorder external web pages inside the same internal web app tab, give them tags like everywhere else, but don't let the user move external web pages under a different internal web app tab than the one that initiated it. Finally, there wouldn't make sense for our rust backend to listen to init and updateResource requests from external web pages, so we'll have our own way to decide what to show for each item in the admin app list: we'll show an icon representing external web site, then on the top row we'll have the title of page when available, and on the bottom row we'll have the url. Let's add a button to copy the url to clipboard (and in case the page gets redirected or the user navigates to change the url, let's also add a button to copy the initial url to clipboard).

## Follow-up 1:

### Regarding the management of external web sites:

1. Let's give the option to suspend or close external web pages: suspending will close the window it is open in. Closing will do the same and will also remove its entry from our list. Suspending the window of a tab will cause the suspending of all child windows containing external web pages. Closing a tab will likewise cause the closing of the same. The same will happen when deleting a tabs group: all external web page windows from all tabs from that group will be closed (we can have external web page windows opened for only one tab at any moment anyway).

### Regarding editing a text file:

The state of editing a text file should be persisted as such and reflected in the tab from the admin app.

### Then a new feature:

Right now we only allow html files to be opened as user apps. Let's also allow markdown files to be opened as user apps. As opposed to html files, whose contents are simply served as they are to the browser, the contents of markdown files will have to be converted to html on the rust backend first and the resulting html will be sent to the browser. And because markdown is html compatible, I would also like to allow style tags and (if possible) script tags to be parsed and interpretted correctly. And for css import statements and external script tags, when the browser requests such external files using a relative file path, can our rust backend fetch those too? The same issue arises for the html files we already open as user apps. And I haven't checked, but html pages (and markdown files) should be openable as web apps from under the Notes app also (list them as separate tabs in the same tab group from the initiating Notes tab). I think it's pretty easy to do that from Note's local file manager. Then what would be really great to do would be to allow html and markdown files to be opened from the Filen browser. So then the rust backend will fetch the html and markdown files from the Filen api (if not yet cached or cache expired), cache them, then serve them from the cache (and display them in the list like we'll display html and markdown files from the Note's file manager).

### Questions:

1. About the response from rust backend to the web page's init request: do we also send the relative path of the requesting html page as part of that response? We should. And when opening from Filen browser we can send them the path from the Filen drive. And when opening them from the notes app, we should also tell them that: they're being opened from the Notes app, not directly from the admin app (instead of a boolean property we should have an enum like property for this, or even a string property whose values are enum member names). Even more so, when opened from the Filen browser, not only will we tell them they're opened from the Notes app, we'll tell them that they're opened from Filen cloud storage, tell them the account id and email, and even the current branch name if such is the case.

2. About web apps: do we have a way to view and copy to clipboard the resource identifier? We should.