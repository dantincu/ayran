1. Regarding the persisted app state for our admin: right now the state of each tab page is persisted while we navigate inside it, but when we go to another tab page, the navigation from the first tab page is lost. Let's keep it: each tab page will have its own state that will be persisted in parallel (and if necessary add checks for tab page state validity)

2. Regarding the paginated list in our apps: besides the digits for prev/next page, let's add a way for the user to see all pages (like a popover listing all page numbers) and pressing on one of them will navigate directly to that page.

3. Let's add keyboard shortcuts for our apps:
   - a shortcut that opens a popover from everywhere inside our admin app that shows the list of tabs with a number in front of each other and a text box that will be readily focused that accepts digits and pressing enter will navigate to that tab page.
   - for all the pages where we have lists add keyboard navigation using arrows: up and down arrows will go to next/prev item in the list, home and end will go to first/last item in the list, pgup and pgdown will go 10 items up or down. Then the left arrow will go to parent and the right arrow will go to currently focused child item.
   - everywhere we have paginated lists we should have a keyboard shortcut that opens the popover that lists the page numbers. Opening this popover with the keyboard shortcut will also make it show a numeric textbox for typing the page number and pressing enter will go to that page (but this textbox won't be visible when opening this popover by pressing on the button with finger or mouse)

4. Regarding our Filen explorers:
   - when saving a file, first check if the version on the server is newer than that I've been working on and warn me and give me 3 options: overwrite, discard changes (will be lost) and cancel (will go back to prev state just like I never hit the save button).

5. Regarding our Filen explorer from the Notes app:
   - give the option to create a new branch from the text editor page
   - give the option to lock a file against caching (disable cache invalidation just for a single file) both in the main branch and in other branches.

6. Regarding branches in Filen explorer from the Notes app:
   - I assume we're only commiting files that we edit, not all files that are cached inside the branch. So let's add an option to simply checkout a file without editing it. This way it will appear in the list of pending changes.
   - Here too we should check that the server version is not newer than the version we've been working on for each file.